// The boundary that actually fires: the existence check lives in
// `[ticker]/layout.tsx`, and a layout's `notFound()` is caught one level up.
export { default } from '@/components/stocks/NotInCoverage';
